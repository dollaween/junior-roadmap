<div align="center">

# Junior Frontend Roadmap

[Главная](https://github.com/dollaween/junior-roadmap/)
[Главная](/)
[Главная](#)
[Главная]()
|
[Карта](/roadmap/README.md)
|
[Тесты](/tests/README.md)
|
[Задачи](/tasks/README.md)

</div>

---

### Javascript
1. Что такое `console.log()`
2. Что такое `Number`
3. Что такое `let`, `const`, `var`
4. Как работают операторы `+`, `-`, `*`, `/`, `**`, `%` с `Number`
5. Как работает метод `toFixed()`
6. Что такое `Math` и как работают его методы `abs()`, `floor()`, `ceil()`, `round()`, `max()`, `min()`, `random()`
7. Что такое `String`
8. В чем разница между кавычками `''`, `""` и ``
9. Как обрабатываются спецсимволы в строке
10. Как сравнивать строки
11. Как работает оператор `+` со `String`
12. Как работает оператор `+` со `String` и `Number`
13. Как работают функция `parseInt()` и `parseFloat()`
14. Как работают методы поиска строки: `charAt()`, `includes()`, `startsWith()`, `endsWith()`
15. Как работают методы строки `toLowerCase()`, `toUpperCase()`, `trim()`, `replace()`, `slice()`, `substring()`, `split()`
16. Что такое `NaN`
17. Как работает функция `isNaN()`
18. Что такое `Boolean`
19. Как работают логические операторы `&&`, `||`, `!`
20. Что такое `undefined`
21. Что такое `null`
22. В чем отличие операторов `==` и `===`
23. Как работает конструкция `if … else`
24. Как работает конструкция `... ? ... : ...`
25. Как работает конструкция `switch`
26. Как работает оператор `typeof`
27. Приведение примитивных типов
28. Что такое `Array`
29. Как происходит доступ к элементам массива и как работает свойство `length`
30. Как работает цикл `for (...) {}`
31. Как работают операторы `break` и `continue`
32. Как работает цикл `while (...) {}`
33. Как работают методы обхода массива: `forEach()`, `map()`, `every()`, `some()`, `filter()`, `reduce()`
34. Как работают методы изменения массива: `push()`, `pop()`, `shift()`, `unshift()`, `reverse()`, `splice()`
35. Как работают методы доступа массива: `indexOf()`, `lastIndexOf()`, `concat()`, `join()`, `slice()`, `toString()`
36. Что такое цепь вызовов
37. Как отсортировать массив
38. Что такое `Object`
39. Что такое перечисляемые и не перечислимые свойства объекта
40. Как сделать свойство объекта не перечислимым
41. Как работают методы объекта: `keys()`, `values()`, `entries()`, `assign()`, `getOwnPropertyNames()`, `hasOwnProperty()`
42. Как работает цикл `for ... of`
43. Как работает цикл `for ... in`
44. Как сравнить два массива или объекта
45. Что такое деструктуризация
46. Что такое остаточные параметры
47. Приведение сложных типов
48. Что такое `function`
49. Что такое анонимная функция
50. Что такое немедленно вызываемая функция
51. Что такое чистая функция
52. Как задать параметры функции по-умолчанию
53. Что такое `this`
54. Что такое стрелочная функция и чем отличается от обычной
55. Что такое `arguments`
56. Что такое псевдомассив и как его преобразовать в массив
57. Что такое `bind()`
58. Что такое `call()` и `apply`
59. Что такое `prototype`
60. Что такое `__proto__`
61. Как работает функция `constructor()`
62. Как работает оператор `new`
63. Как работает функция `super()`
64. Как работает оператор `in`
65. Как отловить ошибки в коде
66. Как работает оператор `debugger`
67. Что такое лексическое окружение и область видимости
68. Что такое переменная
69. Что такое глобальная и локальная переменные
70. Что такое `class`
71. Как работают ключевые слова `get`, `set` в классе
72. Что такое `static` в классе
73. Как работает оператор `extends`
74. Как работает оператор `instanceof`
75. Что такое наследование классов
76. Что такое `Promise`
77. Что означают состояния промиса `pending`, `fulfilled`, `rejected`
78. Как работают методы промиса `resolve()`, `reject()`, `then()`, `catch()`, `finally()`, `all()`, `any()`, `race()`
79. Что такое `async ... await`
80. Как работает конструкция `try ... catch ... finally`
81. Что такое `Error`
82. Как работает оператор `threw`
83. Что означают типы ошибок `TypeError`, `SyntaxError`, `ReferenceError`, `RangeError`, `InternalError`
84. Что такое контекст выполнения
85. Что такое цикл событий
86. Что такое стек вызовов
87. Что такое поднятие
88. Что такое затенение переменных
89. Что такое замыкание
90. Что такое макрозадачи и микрозадачи
91. Что такое утечка памяти
92. Как устроен процесс сборки мусора в современных браузерах
93. Что такое `window`
94. Что такое `document`
95. Что такое `body`
96. Что такое DOM
97. Как работает поиск `getElement*` и `querySelector*`
98. Как работают свойства `innerHTML`, `outerHTML`, `dataset`, `attributes`
99. Как работают методы `hasAttribute()`, `getAttribute()`, `setAttribute()`, `removeAttribute()`
100. Как работают методы `createElement()`, `cloneNode()`, `append()`, `remove()`
101. Как работают свойства `className`, `classList` и метод `getComputedStyle()`
102. Как работают свойства `offsetParent`, `offsetLeft/Top`, `offsetWidth/Height`, `clientTop/Left`, `clientWidth/Height`, `scrollWidth/Height`, `scrollLeft/Top` и методы `getBoundingClientRect()`, `scrollTo()`, `scrollBy()`, `scrollIntoView()`
103. Что такое событие
104. Как работают методы `addEventListener()` и `removeEventListener()`
105. Что такое всплытие и перехват событий
106. Что такое `e.preventDefault()` и `e.stopPropagation()`
107. Что такое `e.target` и `e.currentTarget`
108. Как вызвать `MouseEvent` (`click`, `dblclick`, `mouseup`, `mousedown`, `mouseenter`, `mouseleave`, `mouseover`, `mouseout`) и какие у него есть свойства
109. Как вызвать `WheelEvent` и какие у него есть свойства
110. Как вызвать `KeyboardEvent` и какие у него есть свойства
111. Как вызвать `InputEvent` и какие у него есть свойства
112. Как вызвать `DragEvent` и какие у него есть свойства
113. Что такое URL
114. Что такое SearchParams
115. Как работают функции `encodeURI`, `decodeURI`, `encodeURIComponent`, `decodeURIComponent`
116. Что такое HTTP, HTTPS
117. Что такое Fetch
118. Что такое заголовки
119. Что такое REST API
120. Как сделать get, post, put, delete запросы
121. Как отправить форму
122. Что такое CORS
123. Что такое FormData
124. Что такое JSON
125. Как работают методы `JSON.stringify()` и `JSON.parse()`
126. Как отправить форму / JSON / File / изображение на сервер
127. Что такое `Date`
128. Как работают методы даты: `now()`, `getFullYear()`, `getMonth()`, `getDate`, `getHours()`, `getMinutes()`, `getSeconds()`, `getMilliseconds()`, `getDay()` и их `set`-аналоги, `setTime()`, `parse()`
129. Что такое `UTC`-время
130. Как работают функции `setTimeout()`, `setInterval()`, `clearTimeout()`, `clearInterval()`
131. Что такое `localStorage` и `sessionStorage`
132. Как работают методы `setItem()`, `getItem()`, `removeItem()`, `clear()`, `key()` и свойство `length`
133. Что такое `Cookie`
134. Что такое `IndexedDB`
135. Что такое `File`
136. Что такое `Blob`
137. Что такое регулярное выражение
138. Как работают методы `match()`, `search()`, `replace()`
139. Что такое рекурсия
140. Что такое `iframe`
141. Что такое Shadow DOM
142. Что такое ООП
143. Что такое функциональное программирование
144. Что такое DRY, SOLID и KISS


### React
1. Что такое React и зачем он нужен
2. Что такое `Shallow DOM`
3. Что такое JSX
4. Как происходит рендеринг элементов
5. Какие есть отличия React на хуках от классов
6. Какой у компонента жизненный цикл
7. Как обрабатываются события у тегов
8. Что такое фрагмент
9. Как работает хук `useState()`
10. Как работает хук `useEffect()`
11. Как работает хук `useRef()`
12. Как работает хук `useCallback()`
13. Как работает хук `useLayout()`
14. Как работает хук `useContext()`
15. Как работает хук `useMemo()`
16. Что такое HOC


### Redux
1. Что такое Redux


### Тестирование
1. Что такое Jest
2. Как работают функции `describe`, `it`
3. Что такое React Testing Library


### HTML
1. Что такое DOM
2. Как работают теги
	* `<html>`
	* `<head>`
	* `<title>`
	* `<style>`
	* `<link>`
	* `<body>`
	* `<script>`
	* `<div>`
	* `<header>`, `<footer>`, `<nav>`, `<section>`
	* `<ul>`, `<ol>`, `<li>`
	* `<h1>`–`<h6>`
	* `<p>`, `<span>`
	* `<a>`
	* `<b>`, `<strong>`, `<i>`, `<u>`
	* `<center>`
	* `<img>`
	* `<form>`
	* `<label>`
	* `<input>`
	* `<select>`
	* `<option>`
	* `<table>`, `<thead>`, `<tbody>`, `<tfoot>`, `<tr>`, `<td>`
3. Что такое BOM


### CSS
1. Что такое CSSOM
2. Как работают свойства:
	* `background`
	* `background-image`
	* `border`
	* `box-shadow`
	* `box-sizing`
	* `color`
	* `cursor`
	* `display`
	* `font`
	* `letter-spacing`
	* `line-height`
	* `list-style`
	* `margin`
	* `opacity`
	* `outline`
	* `overflow`
	* `padding`
	* `position`
	* `text-align`
	* `text-decoration`
	* `text-shadow`
	* `transform`
	* `transition`
	* `top`, `bottom`, `left`, `right`
	* `vertical-align`
	* `white-space`
	* `width`, `height`, `max-width`, `max-height`, `min-width`, `min-height`
	* `z-index`
3. Как работают правила:
	* `@font-face`
	* `@import`
	* `@media`
4. Как работают псевдоклассы:
	* `:active`
	* `:first-child`, `:last-child`, `:nth-child`
	* `:focus`
	* `:hover`
5. Как работают селекторы:
	* `#id`
	* `.class`
	* ` `, `>`, `+`, `~`
6. Как работают псевдоэлементы:
	* `:after`
	* `:before`
7. Как работает функция `calc()`
8. Почему inline стили это плохо
9. Что такое препроцессоры
10. Что такое SASS/SCSS
11. Что такое BEM


### Зависимости
1. Что такое Git
2. Что такое NPM
3. Что такое Yarn



